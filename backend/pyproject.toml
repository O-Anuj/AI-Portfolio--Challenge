[project]
name = "ai-portfolio-backend"
version = "0.1.0"
description = "AI Portfolio Challenge — FastAPI backend with Groq streaming"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "groq>=1.6.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
]
